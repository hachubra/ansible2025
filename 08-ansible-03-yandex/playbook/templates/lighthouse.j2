server {
    listen       80;
    server_name  localhost;

    access_log /var/log/nginx/{{ logs }}_access_log;
    error_log /var/log/nginx/{{ logs }}_error_log;

    location / {
        root   {{ dir }};
        index   index.html;
    }
}